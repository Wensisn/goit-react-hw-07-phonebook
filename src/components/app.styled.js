import styled from 'styled-components';

export const Bosx = styled.div`
background-color: white;
    padding: 30px 40px;
    margin: 10px auto;
    width: 450px;
    display: grid;
    gap: 30px;
box - shadow: var(--primary - box - shadow);
`;
